# mood-tracker

## Running Expo

- Auto mode (OS-aware): `npm start`
- Force tunnel: `npm run start:tunnel`
- Force LAN: `npm run start:lan`

The auto launcher uses tunnel on Windows and LAN on Linux/macOS by default.